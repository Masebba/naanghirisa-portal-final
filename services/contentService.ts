import { getPageContent, updatePageContent, getSystemSettings, updateSystemSettings, subscribeStoreUpdates } from './mockData';

export const contentService = {
  getPageContent,
  updatePageContent,
  getSystemSettings,
  updateSystemSettings,
  subscribeStoreUpdates,
};
